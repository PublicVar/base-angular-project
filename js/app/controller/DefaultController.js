(function () {

    angular.module('project').controller('DefaulController', [ "$scope", "CONSTANT_EVENTS",
        function ($scope,CONSTANT_EVENTS) {
            

            function init() {
               
            }

            init();

        }]);

})();